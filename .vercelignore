# Local media that must never be uploaded to Vercel (large source files).
# Processed, web-optimized versions live in public/hero and public/home.
# NOTE: paths are root-anchored (leading slash) so they do NOT match
# public/photos on case-insensitive filesystems.
/Photos
/DTS_*.mov
/DTS_*.mp4
/reference
